%h1 Ideabox
%h2 Showing all ideas in time slot ##{slot}
%ul
  - if !ideas.empty?
    - ideas.each do |idea|
      %li
        = idea.title
        %br/
        = idea.description
        %br/
        Tagged with:
        - idea.tags.each do |tag|
          %a{href: "/tags/#{tag}"} #{tag}
  - else
    No ideas for this time slot
