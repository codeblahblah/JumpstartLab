%h1 Ideabox
%h2 Showing all ideas on:
%ul
  - ideas.each do |idea|
    %li
      = idea.title
      %br/
      = idea.description
      %br/
      Tagged with:
      - idea.tags.each do |tag|
        %a{href: "/tag/#{tag}"} #{tag}

%p #{ideas.to_s}
